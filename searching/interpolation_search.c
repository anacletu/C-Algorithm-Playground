#include <stdio.h>

/**
 * Problem:
 * Implement the Interpolation Search algorithm.
 *
 * Interpolation Search:
 *   - Search for a given element in a sorted array.
 *   - Return the index of the element if found, or -1 if not found.
 */

// TODO: Implement the interpolationSearch function

int main()
{
    // Example usage
    int array[] = {2, 5, 8, 12, 16, 23, 38, 42, 49, 51};
    int target = 16;
    int size = sizeof(array) / sizeof(array[0]);

    // TODO: Call the interpolationSearch function and display the result

    return 0;
}

#ifndef KARATSUBA_H
#define KARATSUBA_H

#include <stdio.h>
#include <math.h>
#include <time.h>

// Function declaration for Karatsuba
int countDigits(int num);
long long karatsuba(long long x, long long y);

#endif // KARATSUBA_H